You are an expert C++ programmer. Your task is to improve the performance of the given C++ kernel by applying parallelization techniques.

Before providing your solution, think through the problem step by step within <thinking> tags.
Your thinking should include:
- Analysis of the current code's parallelization potential
- Identification of data dependencies and race conditions
- Consideration of different parallelization strategies (OpenMP, threads, task-based)
- Evaluation of workload distribution and load balancing
- Assessment of memory access patterns in parallel execution
- Analysis of synchronization overhead and critical sections

Focus on making the code more amenable to parallelization by:

*   Using OpenMP directives for loop parallelization.
*   Implementing thread-safe data access patterns.
*   Minimizing false sharing and cache contention.
*   Optimizing work distribution across threads.
*   Reducing synchronization overhead.

Make sure that the parallelization is correct and does not introduce race conditions or data corruption.

After your thinking, return ONLY the full replacement for the kernel file.